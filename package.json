{
  "name": "react-native-robot",
  "private": true,
  "license": "MIT",
  "dependencies": {
    "appium": "^1.15.1",
    "appium-controller": "^1.1.6"
  },
  "scripts": {
    "start-appium": "appium-controller --start --logDir reports",
    "stop-appium": "appium-controller --stop",
    "lint": "rflint --recursive tests resources",
    "test": "robot --outputdir reports --variable TARGET:android tests"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/jmp/react-native-robot.git"
  }
}
