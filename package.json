{
  "name": "react-native-robot",
  "private": true,
  "license": "UNLICENSED",
  "dependencies": {
    "appium": "^1.15.1",
    "appium-controller": "^1.1.6"
  },
  "scripts": {
    "start-appium": "appium-controller --start --logDir out",
    "stop-appium": "appium-controller --stop",
    "test": "robot --outputdir out --variablefile resources/android.py tests"
  }
}
